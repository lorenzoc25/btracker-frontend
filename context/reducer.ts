import { Action, State } from '../types/context';

export const reducer = (
  state: State,
  action: Action,
) => {
  switch (action.type) {
    case 'ADD':
      break;

    default:
      break;
  }
};
